---
title: What to Eat With
layout: base.njk
---

<h2>What to Eat With</h2>
<p>Find the perfect pairings for your favorite foods.</p>

{% set latest = collections.post | reverse | first %}
{% if latest %}
  <div class="post-card">
    <h3><a href="{{ latest.url }}">{{ latest.data.title }}</a></h3>
    <p>{{ latest.date | date("MMMM d, yyyy") }}</p>
    <p>{{ latest.templateContent | safe }}</p>
  </div>
{% endif %}

<h3>More Posts</h3>
<ul>
  {% for post in collections.post | reverse %}
    {% if not loop.first %}
      <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a> – {{ post.date | date("MMMM d, yyyy") }}
      </li>
    {% endif %}
  {% endfor %}
</ul>
